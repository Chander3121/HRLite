<!DOCTYPE html>
<html>
  <head>
    <title>HRLite</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", defer: true %>
  </head>

  <body class="min-h-screen bg-gray-50 flex items-center justify-center px-4">
    <div class="w-full max-w-md">
      <div class="bg-white rounded-2xl shadow-sm border border-gray-200 p-8">
        <div class="mb-6 text-center">
          <h2 class="text-xl font-bold text-indigo-600">HRLite</h2>
        </div>

        <% flash.each do |type, message| %>
          <div class="mb-4 rounded-lg px-4 py-2 text-sm
            <%= type == "notice" ? "bg-green-100 text-green-700" : "bg-red-100 text-red-700" %>">
            <%= message %>
          </div>
        <% end %>

        <%= yield %>
      </div>

      <p class="mt-6 text-center text-xs text-gray-400">
        © <%= Time.current.year %> HRLite. All rights reserved.
      </p>
    </div>
  </body>
</html>
