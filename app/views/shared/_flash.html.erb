<% flash.each do |type, message| %>
  <div class="mb-4 px-4 py-3 rounded-lg
    <%= type == "notice" ? "bg-green-100 text-green-700" : "bg-red-100 text-red-700" %>">
    <%= message %>
  </div>
<% end %>
